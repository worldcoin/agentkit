---
"@worldcoin/agentkit": minor
"@worldcoin/agentkit-cli": minor
---

Add an EVM-only `createAgentkitClient` pre-payment client for x402 calls and an `agentkit status` command for AgentBook registration checks.
