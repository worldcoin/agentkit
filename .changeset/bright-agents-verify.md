---
"@worldcoin/agentkit": minor
"@worldcoin/agentkit-cli": minor
---

Add an EVM-only `createAgentkitFetch` pre-payment helper for x402 calls and an `agentkit status` command for AgentBook registration checks.
